# The following environment variables are set by a shell/batch

# script on the environment for which the build will be run

# 

#

# WLS70_HOME

# JDK_HOME

# ANT_HOME

# DOC_HOME



build.root = ../../

app.name = Studymanagement_WebApplication

app.root = ../../${app.name}

src.dir    = ../src





build.dir  = .

lib.dir    = ../WebContent/WEB-INF/lib

syslib.dir = ../syslib







#javadoc.dir = ${env.DOC_HOME}/javadoc/customercare

javadoc.dir = ../doc

wartmp.dir = ./tmpwar

wars.dir    = ../wars

webContent.dir=../WebContent

JDK_HOME.dir = C:/Program Files/Java/jdk1.7.0_45

java.bin = ${env.JDK_HOME}/bin

javac.cmd = ${java.bin}/javac

jvm.cmd = ${java.bin}/java

xjc.cmd = ${java.bin}/xjc

wsimport.cmd = ${java.bin}/wsimport

ant.dir= ${env.ANT_HOME}

m360.xsd=../src/main/xsd/m360.xsd

megaservice.xsd = ../src/main/xsd/megaservice.xsd
sylantro.xsd=../src/main/xsd/sylantro.xsd
# configurable test environment

testproperty.dir = ${build.dir}/properties/local
tmpalloc.dir = ${build.dir}/tmpalloc

# Siebel webservice WSDL and XSD directories

wsdl.dir    =../wsdl

smartallocurl = ${wsdl.dir}/IPAllocWebService.wsdl

smartallocpackage=net.megapath.smartalloc
